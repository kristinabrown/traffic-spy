<h1><%= @source.identifier %></h1>

<h2>Most Requested to Least Requested events:</h2>

<%= partial(:'partials/event', collection: @events) %>

<ul>
  <% @source.unique_events.each do |event| %>
    <li><a href="/sources/<%= @source.identifier %>/events/<%= event %>" ><%= event %></a></li>
  <% end %>
</ul>